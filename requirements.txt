# =============================================================================
# AdvisorAI — Backend dependencies
# =============================================================================
# FREE services used (no credit card needed for development):
#   ✅ Groq        — free 14,400 req/day   → LLM inference
#   ✅ SentenceTransformers — free, local  → embeddings (zero API cost)
#   ✅ PostgreSQL + pgvector — self-hosted → vector database
#   ✅ Redis         — self-hosted          → cache + task queue
#   ✅ MinIO         — self-hosted          → S3-compatible file storage
#   ✅ spaCy         — open source          → NLP / entity extraction
#   ✅ scikit-learn  — open source          → ML models
#
# Optional paid services (substitutable with free alternatives):
#   💰 OpenAI → disabled by default; use Groq (free)
#   💰 AWS S3 → use MinIO in docker-compose.yml (free, self-hosted)
#   💰 Sentry → commented out; free tier available at sentry.io
# =============================================================================

# ── Core framework ────────────────────────────────────────────
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic>=2.5.3
pydantic-settings==2.1.0

# ── Database (PostgreSQL + pgvector) ─────────────────────────
sqlalchemy==2.0.25
alembic==1.13.1
psycopg2-binary==2.9.9
asyncpg==0.29.0
pgvector==0.2.4

# ── Auth & security ───────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6
bcrypt==4.1.2

# ── Cache & async task queue (Redis + Celery) ────────────────
redis==5.0.1
celery==5.3.6
flower==2.0.1

# ── AI / LLM — Groq FREE tier (14,400 req/day) ───────────────
groq>=0.4.0
langchain>=0.1.4
langchain-text-splitters>=0.3.0
langchain-groq>=0.1.0

# ── Embeddings — 100% LOCAL, zero API cost ───────────────────
sentence-transformers==2.3.1
torch==2.2.0

# ── NLP — open source ─────────────────────────────────────────
spacy==3.7.2
rank-bm25==0.2.2
python-dateutil>=2.8.2
tiktoken==0.5.2

# ── ML — open source ──────────────────────────────────────────
scikit-learn==1.4.0
joblib==1.3.2
numpy==1.26.3
pandas==2.2.0

# ── Document parsing ──────────────────────────────────────────
pypdf==4.0.1
python-docx==1.1.0
openpyxl==3.1.2
python-magic==0.4.27
aiofiles==23.2.1

# ── File storage (MinIO = free self-hosted; AWS S3 = paid) ───
boto3==1.34.34
botocore==1.34.34

# ── Report generation ─────────────────────────────────────────
reportlab==4.1.0

# ── HTTP client (webhooks, outbound requests) ────────────────
httpx==0.26.0

# ── Email / validation ────────────────────────────────────────
email-validator==2.1.0

# ── Utilities ─────────────────────────────────────────────────
python-dotenv==1.0.0
python-slugify==8.0.1

# ── Logging ───────────────────────────────────────────────────
python-json-logger==2.0.7

# ── Testing ───────────────────────────────────────────────────
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
faker==22.2.0

# ── Optional: Sentry error monitoring (free tier at sentry.io)
# Uncomment to enable: sentry-sdk[fastapi]==1.40.0

# ── Optional: OpenAI fallback (paid; only if GROQ_API_KEY unset)
# openai>=1.10.0
# langchain-openai>=0.0.5
